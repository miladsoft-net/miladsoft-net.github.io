---
import WidgetLayout from "./WidgetLayout.astro";
import ButtonLink from "../control/ButtonLink.astro";
import { Icon } from "astro-icon/components";

interface Props {
  class?: string;
  style?: string;
}

const { class: className, style } = Astro.props;
const COLLAPSED_HEIGHT = "5rem";
const ITEMS_THRESHOLD = 3; // Show More button after 3 items
---

<WidgetLayout
  name="Shopping Cart"
  id="cart-widget"
  class={className}
  style={style}
  isCollapsed={true}
  collapsedHeight={COLLAPSED_HEIGHT}
>
  <div class="flex flex-col gap-2">
    <div id="cart-items" class="flex flex-col gap-2 mb-3">
      <div class="text-sm text-neutral-600 dark:text-neutral-400">
        Loading...
      </div>
    </div>

    <button
      id="more-button"
      class="hidden text-sm font-medium text-primary-600 hover:text-primary-700 dark:text-primary-400 dark:hover:text-primary-300 transition-colors"
    >
      Show More
    </button>
  </div>

  <div
    class="flex flex-col gap-3 border-t pt-3 mt-2 border-neutral-200 dark:border-neutral-800"
  >
    <div class="flex justify-between items-center">
      <span class="text-sm font-medium text-neutral-700 dark:text-neutral-300"
        >Total:</span
      >
      <span id="cart-total" class="font-bold text-neutral-900 dark:text-white"
        >$0.00</span
      >
    </div>

    <a href="/cart/">
      <button
        id="checkout-button"
        class="w-full py-3 mt-4 bg-[var(--primary)] text-white rounded-xl font-semibold
               transition-all duration-300 ease-in-out hover:opacity-90 hover:-translate-y-1 hover:shadow-lg
               disabled:opacity-50 disabled:cursor-not-allowed disabled:hover:translate-y-0
               disabled:bg-gray-400 dark:disabled:bg-gray-700 flex items-center justify-center gap-2"
      >
        <Icon
          name="material-symbols:shopping-cart-checkout"
          class="w-6 h-6 sm:w-6 sm:h-6"
        />
        <span>View Cart & Checkout</span>
      </button>
    </a>
  </div>
</WidgetLayout>

<script>
  import { cart } from "../../store/cartStore";

  const cartItemsElement = document.getElementById("cart-items");
  const cartTotalElement = document.getElementById("cart-total");
  const checkoutButton = document.getElementById("checkout-button");
  const moreButton = document.getElementById("more-button");
  let isExpanded = false;

  moreButton?.addEventListener("click", () => {
    isExpanded = !isExpanded;
    updateCartDisplay();
  });

  function updateCartDisplay() {
    if (!cartItemsElement || !cartTotalElement || !checkoutButton || !moreButton) return;

    cart.subscribe((items) => {
      if (items.length === 0) {
        cartItemsElement.innerHTML = '<div class="text-sm text-neutral-500 dark:text-neutral-400">Cart is empty</div>';
        cartTotalElement.textContent = "$0.00";
        checkoutButton.classList.add("opacity-50", "cursor-not-allowed");
        moreButton.classList.add("hidden");
        return;
      }

      checkoutButton.classList.remove("opacity-50", "cursor-not-allowed");
      const total = items.reduce((sum, item) => sum + (item.salePrice || item.price), 0);
      cartTotalElement.textContent = `$${total.toFixed(2)}`;

      // Only slice if there are more than 3 items and not expanded
      const displayItems = (items.length > 3 && !isExpanded) ? items.slice(0, 3) : items;

      cartItemsElement.innerHTML = displayItems
        .map((item) => `
          <a href="/posts/${item.slug}/" class="block w-full h-10 rounded-lg bg-none hover:bg-[var(--btn-plain-bg-hover)] active:bg-[var(--btn-plain-bg-active)] transition-all pl-2 hover:pl-3">
            <div class="flex justify-between items-center h-full">
              <span class="truncate flex-1 text-neutral-700 hover:text-[var(--primary)] dark:text-neutral-300 dark:hover:text-[var(--primary)]">
                ${item.title}
              </span>
              <span class="font-medium ml-4 mr-2 min-w-[4rem] text-right text-neutral-700 hover:text-[var(--primary)] dark:text-neutral-300 dark:hover:text-[var(--primary)]">
                $${(item.salePrice || item.price).toFixed(2)}
              </span>
            </div>
          </a>
        `).join("");

      // Only show More button if items exceed 3
      if (items.length > 3) {
        moreButton.classList.remove("hidden");
        moreButton.textContent = isExpanded ? "Show Less" : `Show More (${items.length - 3})`;
      } else {
        moreButton.classList.add("hidden");
      }
    });
  }

  updateCartDisplay();
</script>
